{
  "name": "cannaq",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.16.0",
  "scripts": {
    "check:env": "node --env-file-if-exists=.env scripts/check-env.mjs",
    "test": "pnpm --filter @workspace/scripts exec tsx --test ../artifacts/api-server/tests/core.test.ts ../artifacts/api-server/tests/onboarding.test.ts",
    "preinstall": "node scripts/check-package-manager.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "start": "pnpm --filter @workspace/api-server start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:web": "pnpm --filter @workspace/cannaqms dev",
    "db:push": "pnpm --filter @workspace/db push"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
